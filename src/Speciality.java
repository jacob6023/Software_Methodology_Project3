package Project_1.src;

public enum Speciality {
    /* this enum class defines the providers' specialties with an additional proprty "charge", which defines the charge per visit*/
    private final int charge;
}
